node_modules/
.env
.env.local

# Never commit vaults, backups, attachments or keys
*.enpassdb
*.enpassdb.backup-*
*.enpassbackup
*.enpassattach
*.keyfile
vault.json

# ...except the genuine SQLCipher test fixtures (dummy data, public password)
!test/fixtures/*.enpassdb
!test/fixtures/*.enpassattach

# OS / editor noise
.DS_Store
*.log
